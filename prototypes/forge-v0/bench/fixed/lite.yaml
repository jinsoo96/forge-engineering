# Fixed benchmark — read-only for Smith. Modifying this file is prohibited by
# FORGE.md locked_surface.
#
# J(H) is the mean of per-task scores. A task may declare:
#   - `score` (static, for legacy demo cases), or
#   - `pytest_dir` (run pytest there; score = passed/total).

baseline: 0.0

tasks:
  - id: sample_repo_pytest
    description: |
      Score = fraction of pytest tests passing in demo/sample_repo. This is the
      empirical anchor for J(H). Initial state: 3/6 pass (J=0.5).
    pytest_dir: demo/sample_repo
