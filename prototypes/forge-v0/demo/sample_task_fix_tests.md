# Task: Make all tests pass

The Python library in `src/sample_app/` ships with bugs. Your job is to fix them
until `pytest` runs clean (all tests pass).

Acceptance criteria:
- Running `pytest` from this directory shows 0 failures and 0 errors.
- Do not modify any files under `tests/`.
- Do not modify `README.md` or `CLAUDE.md`.
- Make the minimum changes needed.
